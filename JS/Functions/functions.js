// In JS, functions are Function objects and first-class objects: they can have properties and methods just like any other object. What distinguishes them from other objects is that functions can be called.

// determining whether a function exists 
if (typeof window.noFunc === 'function') { 
	// noFunc() // use noFunc
	} else { 
	// do something else 
}