// ! .reverse()

// transposes the elements of an array, in place: 
// the first element becomes the last and the last becomes the first and returns a reference to the array 
var myArray = new Array('1', '2', '3'); 
myArray.reverse(); 
console.log(myArray); // ["3", "2", "1"]